# 共通分析フレームワーク

レポート執筆開始時に最初に読むこと。全ての分析ファイル（map_reading.md / cross_module.md / patent_citation.md）の基盤となる。

---

## 1. 4層分析モデル

全ての分析は以下の4層を必ず経由すること。Layer 1だけで終わるレポートは「事実の列挙」でしかない。

| Layer | 名称 | 内容 | 文章中のマーカー例 |
|-------|------|------|-----------------|
| Layer 1 | 事実 | データから直接読み取れる客観的数値・パターン | 「〜である」「〜件」「〜%」 |
| Layer 2 | 解釈 | 技術的・市場的に何を意味するか | 「これは〜を示唆している」「〜と解釈できる」 |
| Layer 3 | So What? | 戦略的示唆・ビジネスインパクト | 「したがって〜」「戦略的には〜」 |
| Layer 4 | 提言 | 具体的アクション・推奨事項 | 「〜を推奨する」「〜を検討すべきである」 |

> ⚠️ **立場（`narrative_stance`）が Layer 3 の力点と Layer 4 の提言の型を規定する**（self=内省と打ち手／competitor・buyer・supplier=機会・脅威と対抗策／neutral=評価・予測）。呼称を三人称にしただけの「べき論」は不可。**正本: `terminology.md` §6-2-B**（5立場の力点表・NG/OK例）。

### ⚠️ 最重要: 4 層は「思考の枠組み」であり「本文に書くラベル」ではない（絶対遵守）

4 層モデルは執筆者が思考を組み立てるための**内部フレーム**である。**レポート本文に層のラベルや接頭辞を書いてはいけない**。以下を本文に出すのは品質不合格（読者には不自然で、機械生成感・テンプレ感が出る）:

- ❌ **本文禁止トークン**: 「（事実）」「（解釈）」「（So What?）」「（提言）」「（L1）」「（L2: …）」「L1:」「L2:」「事実:」「解釈:」「示唆:」「So what:」「So What:」「経営的含意:」「経営的意味:」「経営的示唆:」「【事実】」「【解釈】」等、層名を明示するラベル全般
- ✅ **正しい書き方**: 4 層を**地の文に自然に溶け込ませる**。数値事実 →（接続）→ その意味 →（接続）→ 戦略的含意 →（接続）→ 具体提言、を「〜であり」「これは〜を意味する」「したがって」「ゆえに〜を推奨する」等の接続表現で **1 つの流れる文章**にする。

### NG例 ①（Layer 1止まり）

> クラスタ3は152件の特許を含み、主要出願人はA社（45件）とB社（32件）である。IPCはH01L21が最多で、2020年以降増加傾向にある。

### NG例 ②（4層は踏んでいるが「ラベルを本文に書いてしまっている」← 頻発・要撲滅）

> クラスタ3は152件で、A社が45件である（事実）。覇権争いが進行中と解釈できる（解釈）。**経営的含意:** ポジション確保が鍵となる（So What）。自社出願の倍増を推奨する（提言）。

### OK例（4層を地の文に溶かす・ラベルは一切書かない）

> クラスタ 3 は 152 件の特許を含み、A 社（45 件、29.6%）と B 社（32 件、21.1%）で過半を占める。両社とも H01L21（半導体製造プロセス）に集中していることから、次世代製造技術の主導権争いが本母集団内で進行していると読み取れる。同クラスタは MEGA PULSE で高成長・高活動の象限に位置し、今後 3〜5 年の出願増が見込まれるため、ここでのポジション確保が競争優位を左右する。具体的には、自社の H01L21 関連出願を年 10 件から 20 件へ倍増させ、とりわけ A 社が手薄なウェットエッチング工程（A 社の 45 件中わずか 3 件）に重点投資することを推奨する。

### 4層モデルの実践チェック

各deep_diveセクションで、以下の自己チェックを行うこと:

1. **L1（事実）が数値付きか**: 「多い」「少ない」等の形容詞は禁止。必ず件数・割合・CAGR等の数値を含める
2. **L2（解釈）に「なぜ」があるか**: 数値変動の因果（政策変更、技術成熟、企業戦略転換等）を論じているか
3. **L3（洞察）に「にもかかわらず」「一見〜だが」があるか**: 単純でない、複数事実を統合した知見を含んでいるか
4. **L4（提言）に企業名・数値目標があるか**: 「検討すべき」だけでなく、誰が・何を・いつまでにを含んでいるか
5. **平易・正確か（過剰修辞の禁止）**: 文芸的な比喩・扇情的な修辞（越境者・狼煙・地殻変動・旗手・苗床・〜の物語 等）を使っていないか。比喩でなく事実・数値で語る（詳細・言い換え例: `terminology.md` §6.5、`phase_d_gate.sh` Check 17）
6. **L2/L3 は対立仮説に耐えるか（ACH）**: 解釈・洞察が「最初に思いついたもっともらしい筋書き」をなぞっただけでないか。中心的な解釈には、実際に支持のあった対立仮説（一社の急増？見かけの空白？大量出願者？）を検討し、**それと食い違う材料**で退けたかを自問する。統合インサイト節・結論章では `#competing-hypotheses` / `#ach-matrix` として外部化する（→ `analysis/structured_techniques.md`。ACH は L2〜L3 の質を担保する仕掛けである）

### 🔍 各 Layer のスコープ明示ルール（絶対遵守）

4 層モデルを使うときに最も頻発するバグが、**「本母集団内の観察」をいつの間にか「業界全体の傾向」として書いてしまう**こと。各 Layer ごとにスコープを以下の通り限定する（詳細は `terminology.md` §6）:

| Layer | スコープ | 書き方ルール |
|---|---|---|
| L1（事実） | **本母集団のみ** | 必ず「本母集団では〜」「本分析の特許群では〜」等の限定修飾を置く。「業界では A 社が 1 位」は NG、「本母集団では A 社が最多出願（152 件、23.4%）」は OK |
| L2（解釈） | **本母集団の解釈** | 同様に母集団限定を維持。「なぜ本母集団でこのパターンが見られるか」を論じる |
| L3（洞察） | 母集団限定が原則、一般化は条件付き | 業界全体への一般化を書くなら、Web 調査の外部裏付けを `#footnote` で必ず付す。裏付けなしに「業界はこの方向」と断定するのは品質不合格 |
| L4（提言） | 提言の対象と根拠を明示 | 「本母集団で観察された動向に基づき、A 社は〜」のように、提言の導出根拠が母集団観察か外部一般化かを明示する |

具体的な NG→OK の書き換え例（無限化の誤読パターン5種）は **`terminology.md` §6-4** を参照（正本）。要は: L1-L2 は「本母集団では〜」で限定し、L3 の一般化は `#footnote` の外部裏付けがある時だけ、L4 は提言の導出根拠（母集団観察か外部一般化か）を明示する。

### 🔒 第1層（事実）への裏付けの要求 — 「事実の装い」を許さない（v9）

誤った主張が最も危険なのは、それが**第1層（事実）の装い**をまとったときである。読者は「事実」として書かれた文を疑わない。よって:

- **第1層（事実）として書く文は、出所をたどれる手がかり（数値＋単位・特許番号・`#footnote`）を同じ文または隣接文に必ず伴わせる**
- 裏付けを添えられない断定——「最大」「最多」「唯一」「支配的」「〜が存在する」「〜が原因で」等——は、**第2層（解釈）以下の言い方に落とす**（「〜とみられる」「〜と解釈できる」「〜と推論する」）。読者が懐疑的に読む姿勢に入れるようにする
- これは**生成時の規律**である。`phase_d_gate.sh` Check 36 は、裏付けの無い断定文を人間の前に並べる**安全網**にすぎない（どの文が第1層かは機械では判定できないため、誤検出前提の WARN）
- 報告書の声は確信度を較正して語る: 「本母集団は〜を示す」（第1層・裏付けあり）／「〜と推論する」（第2層）／「これは我々の読みである」（第3層）を書き分ける

### 🎯 設計意図（query_intent）を 4 層モデルで内在化させる

Phase A STOP-GATE で確定した `query_intent` と `sub_questions`（`reports/_phase_a_decisions.json`）は、**4 層モデルの各層で継続的に意識する**こと。特に Layer 3（洞察）と Layer 4（提言）で意図と乖離した結論を書くと、レポート全体の一貫性が失われる。

| Layer | 設計意図の扱い |
|---|---|
| **L1（事実）** | 意図に直接関係する事実を優先的に取り上げる。ただし意図から外れる事実も観察された場合は隠さず記述（後段で「分析過程で確認された追加的事項」章にまとめる） |
| **L2（解釈）** | 「本分析の視座では〜」等の修飾を Layer 1 → Layer 2 の移行で添えると、意図との接続が自然になる |
| **L3（洞察）** | 意図に対して何を示唆するかを明示する。意図と無関係な洞察を書かない |
| **L4（提言）** | **意図から直結した提言**のみを書く。意図と関係ない一般論的提言は禁止。`sub_questions` の観点がすべて結論章で回答されているかを執筆者メモで自己点検 |

**⚠️ 問い/答え形式の禁止（再掲）**: サブクエスチョン（SQ1 等）はあくまで執筆者の内部メモであり、レポート本文には通常の宣言調の論述を書く（詳細: `analysis/terminology.md` §5-A-2）

### 🎯 母集団タイプ（A/A'/B/C/D）別の 4 層モデル運用

Phase A STOP-GATE C で確定した母集団タイプ（`reports/_phase_a_decisions.json` に記録）に応じて、**4 層モデルで使える指標・使える表現が変わる**。詳細は `analysis/population_type_metrics.md` §2 の指標解釈表と §3 のタイプ別禁止表現リストを参照。

特に注意すべきポイント:

| タイプ | Layer 1（事実） | Layer 3（洞察） の制約 |
|---|---|---|
| **A 業界全体 / A' 技術領域** | 業界/技術領域の数値として解釈可 | 業界全体への一般化が可能（Web 裏付けの条件付き） |
| **B 競合限定** | 「**調査対象 N 社内での**」限定必須 | 「市場集中」「業界寡占」等の表現は**禁止**。「対象社間の非対称性」に限定 |
| **C 単一企業** | 主語は対象企業に限定（出願人 HHI は算出しない）。**呼称は「分析の立場」に従う**（self=「当社」／competitor・neutral=企業名で三人称） | 「市場」「業界」を含む全表現が**禁止**。発明者集中度・IPC 集中度で代替 |
| **D 特定製品・技術テーマ** | 「本テーマでは」限定必須 | 「業界全体では」は禁止。テーマ内の構造として記述 |

NG/OK の具体例（タイプ C で HHI=1.0 を市場寡占と誤読する例、self/neutral の呼称書き分け例）は **`population_type_metrics.md` §3** と **`terminology.md` §6-2-B** を参照（正本）。「当社」と「同社／企業名」を1レポート内で混在させない。

→ 詳細ルール: `analysis/population_type_metrics.md`

**不合格パターン（頻出）:**
- L1の事実列挙だけで1セクションが終わる（クラスタ一覧、出願人ランキング等）
- L2の解釈が1行（「〜を示す」「〜を反映している」で終わる）
- L3が完全に欠落（最も多いパターン）
- L4が汎用的（「今後の動向を注視すべき」「参入を検討すべき」等の抽象表現）

**合格パターン:**
- L1: 「クラスタ0（タイヤ用ゴム、14件）のCAGR +254.5%」
- L2: 「2021年以降の急増はタイヤメーカー（TOYO TIRE）の参入と旭化成のマスターバッチ技術確立が背景」
- L3: 「このCAGR +254.5%はCNF全体の-23.0%と真逆であり、技術の主戦場が基盤材料からゴム応用に構造的に転換していることを意味する。製紙大手はこの転換に乗り遅れている」
- L4: 「製紙大手がこの領域に参入するには、タイヤメーカーとの共同開発契約を2025年中に締結し、CNF×ゴムの配合最適化技術を内製化する必要がある」

---

## 2. 数値根拠の標準書式

全ての主張には具体的な数値根拠を付すこと。

### 件数・割合
- 「A社は152件（全体の23.4%）」— 件数と割合を必ず併記
- 「上位3社で全体の67.2%を占める」— 集中度を示す

### 成長率
- 「CAGR +15.2%/年（2018→2023年、32件→65件）」— 率・期間・始終値を全て含む
- 成長が見られない場合も明記:「CAGR -2.1%/年（停滞期）」

### 集中度
- 「HHI = 0.32（高集中型: 上位2社寡占）」— 値 + 解釈区分
- 解釈基準: < 0.15 分散型 / 0.15-0.25 中程度 / > 0.25 高集中型

### 多様性指標の解釈基準

| 指標 | 算出方法 | 解釈 |
|------|--------|------|
| HHI | Σ(share²) | < 0.10: 競争的、0.10-0.18: 中程度、> 0.18: 寡占 |
| Entropy | -Σ(p × log₂(p)) | 高い=多様（分散）、低い=集中 |
| Gini | 累積分布の不平等度 | 0=完全平等、1=完全不平等 |

3指標を組み合わせることで、単一指標では捉えられない市場構造の特徴を立体的に把握できる。
例: HHI低（競争的）× Gini高（不平等）= 上位少数が支配しつつも多数の小規模プレイヤーが参入している状態。

### ネットワーク指標
- 「媒介中心性 0.45（ネットワーク内最高値）」— 値 + 相対位置
- 「ネットワーク密度 0.18（中程度: 協業は活発だが閉じたクラスタが存在）」

### 期間
- 「直近3年（2021-2023年）」— 常に具体的な年を併記
- 「分析対象期間: 2015-2023年（9年間、総計2,340件）」

---

## 3. 段落構成のパターン

1つのEvidence内の各段落は以下の4文構成を基本とする。

```
[主張文] クラスタ3は半導体製造プロセス革新の最前線である。
[数値根拠] 152件（全体の23.4%）を含み、CAGR +15.2%/年で成長中。A社（45件）とB社（32件）が牽引している。
[特許引用] 代表的なA社特許（特開2022-XXXXXX「次世代EUVリソグラフィ装置」）は同クラスタの技術的方向性を端的に示している。
[含意] この集中は、EUVリソグラフィが次世代半導体製造の標準技術として確立されつつあることを意味する。
```

- 段落の長さ: 4-8文（短すぎず長すぎず）
- 1段落1主題: 1つの段落で複数のクラスタや複数のテーマを混在させない
- 接続: 段落間の論理的つながりを接続詞で明示する（「一方」「これに対し」「さらに」「総合すると」）
- **地の文の連続は4段落まで**: 「第1の発見は…第2の…」式に地の文の段落だけを5つ以上並べない（壁テキスト＝読了率が下がる）。4段落までに表（`styled-table`）・箇条書き・ボックス・小見出しのいずれかで分節する。特に統合・サマリ系の節は**冒頭に統合表を置いてから散文**の順にする（`phase_d_gate.sh` Check 37 が構造化要素なしの3,000字超連続を検出）

---

## 4. マップ（スナップショット画像）の配置ルール — 正本

> 品質チェックリスト（必須要件・段落深度・推奨要件）は **`analysis/quality_checklist.md` に一本化**した（Phase D 必読・gate と対応済み）。本節はレポート全体で参照される**マップ配置ルールの正本**である。

マップは「章の冒頭にまとめて並べる」のではなく、**そのマップを論じる本文段落のすぐ近くに1枚ずつ**置く。図と分析テキストが離れていると、読者は「図はあるが何を読み取ったのか分からない」状態になり、図が単なる飾りになる。各図の直前または直後に、その図から読み取った事実（L1）→解釈（L2）を必ず書く。

> ❌ **NG例（見出し直後にマップを羅列 → 後からまとめて言及）**

```typst
== 3. 技術ランドスケープ分析

#snapshot-figure("saturn_main_snap.png", "全体俯瞰図")
#snapshot-figure("saturn_drill_snap_0.png", "クラスタ0ドリルダウン")
#snapshot-figure("mega_pulse_applicant.png", "MEGA 4象限")

本技術領域は43クラスタに分かれ、上位5クラスタで58%を占める。MEGA分析では
旭化成がリーダー象限に位置する。（← 3枚の図がどれを指すのか本文と対応せず、
図は章頭の飾りになっている）
```

> ✅ **OK例（各マップを、それを論じる段落の近くに1枚ずつ配置）**

```typst
== 3. 技術ランドスケープ分析

Saturn V TELESCOPE分析の俯瞰図を見ると、全2,623件が43クラスタに分かれ、
3つの空間グループを形成している。最大の「液化プロセス群」が図の右下に集中する。

#snapshot-figure("saturn_main_snap.png", "全体俯瞰図")

この最大クラスタ0「液化プロセス」を拡大すると、AIR LIQUIDE等の産業ガス大手の
出願が中核を占めることが分かる。

#snapshot-figure("saturn_drill_snap_0.png", "クラスタ0ドリルダウン")
```

> ❌ **NG例（`grid` で2枚を横並べ・縮小 → 読めなくなる）**

```typst
// 2枚を比較したいからと grid で横に並べる — 禁止
#grid(
  columns: 2, gutter: 8pt,
  snapshot-figure("../snapshots/saturn_main_snap.png", caption: "全体俯瞰図"),
  snapshot-figure("../snapshots/snap_saturnv_dynamics_overview.png", caption: "クラスタ動態マップ"),
)
```

各マップが半分以下に縮小され、クラスタラベル・軸・凡例が読めなくなる。`image(..., width: 48%)` のような幅指定の縮小も同様に禁止。

> ✅ **OK例（比較したい2枚も、1枚ずつ全幅で縦に置き、本文で比較する）**

```typst
全体俯瞰図では43クラスタが3つの空間グループを形成している。

#snapshot-figure("../snapshots/saturn_main_snap.png", caption: "全体俯瞰図")

これをクラスタ動態マップと突き合わせると、空間的に近接するクラスタ1・3が
いずれも成長リーダー象限に位置しており、同一超領域の成長が同期していることが分かる。

#snapshot-figure("../snapshots/snap_saturnv_dynamics_overview.png", caption: "クラスタ動態マップ")
```

**要点**: ①見出し（`==`）の直後の行に `#snapshot-figure()` を置かない（必ず先に本文段落を書く）。②図は1段落＝1枚を目安に分散配置する。③同一画像を複数章から異なる視点で引用するのは可。④冒頭のエグゼクティブサマリー（要約）章や経営層向け別冊（`report_executive.typ`）で掲載したマップを、本編の対応する詳細章で再掲するのは「図の重複」に当たらない（要約・別冊での提示と詳細章での分析は目的が異なるため）。⑤**マップは1枚1行・全幅**。`grid`・`stack` 等での2枚以上の横並べ、`width: <100%` の縮小配置は禁止（比較は本文の言葉で行う）。⑥**図の隣接段落は、指示語でその図を名指しする**——「俯瞰図を見ると〜」「図中の右上に〜」「この図のネットワークでは〜」。**ただし「上図／下図／次の図／以下の図／左図／右図」等の相対位置による参照は禁止**（組版で図と本文の位置関係が変わるとズレ、実際に「上にある図を『下図』と呼ぶ」事故が起きる。名指しは図の名称〈俯瞰図・クラスタ動態マップ等〉＋「図中／この図／〜の図では／〜のマップでは」型で行う）。隣に本文があっても、その本文が図に触れていなければ図は飾りである（データの話を書いて図を置くだけでは不可。図から読み取った事実を最低1つ書く）。`phase_d_gate.sh` が「見出し直後行が `#snapshot-figure` の章」（Check 23）・「横並べ・縮小」（Check 28）・「分析本文が隣接しない図」（Check 29）・「図を名指ししない隣接段落」（Check 29b）を WARN として検出する。

---

## 5. データソース明示ルール（ハルシネーション防止 / 用語統一版）

レポート中のデータ引用には、**具体的なモジュール名・分析手法名**を明示するマーカーを必ず付与する。「マクロ統計データによると」のような抽象的表現は禁止し、読者がデータの出自を正確に追跡できるようにする。

**マーカー中に内部ファイル名（`*.json`）・内部フィールド名（`spatial_context`, `cluster_dynamics`, `quadrant_summary`, `time_series` 等）・内部ガイドファイル名（`*.md`）を書くことを禁止する。** 必ず `analysis/terminology.md` の正式日本語呼称に置き換えること。

### マーカー一覧（内部識別子なし）

| データソース | マーカー例（正しい書き方） | 使用場面 |
|------------|---------------------|---------|
| 特許データを直接参照して分析 | 「**特許データセットの条件検索（クラスタ 3 に絞込）によると**」「個別特許の精査では」「出願人別フィルタリングの結果」 | 特定クラスタの特許を絞り込んで読んだ場合、特定出願人の特許を検索した場合 |
| Saturn V / EAGLE モジュール出力 | 「**Saturn V TELESCOPE 分析（SBERT + UMAP + HDBSCAN）の結果**」「**EAGLE 探索的クラスタリング分析**によると」「**Saturn V TELESCOPE 分析の空間配置**によると」 | クラスタ数・クラスタ構造・空間配置などの分析結果を引用する場合 |
| Explorer モジュール出力 | 「**Explorer の共起ネットワーク分析（Jaccard 係数 ≥ 0.03）の結果**」「**Explorer のコミュニティ検出結果**から」 | 共起頻度・コミュニティ構造・ハブノードなどの分析結果を引用する場合 |
| MEGA モジュール出力 | 「**MEGA PULSE 分析（CAGR × 活動量の 4 象限マッピング）によると**」「**MEGA PULSE 分析の 4 象限サマリ**から」 | 4 象限配置・成長率・動態分析の結果を引用する場合 |
| ATLAS モジュール出力 | 「**ATLAS 時系列分析の結果**」「**ATLAS 基本統計分析のライフサイクル指標**によると」 | 時系列推移・ライフサイクル・ランキングの結果を引用する場合 |
| ATLAS 権利化率分析 | 「**ATLAS 権利化率分析（出願数 × 権利化率の象限）によると**」 | 出願人/技術別の権利化率・出願数の象限（量質バランス）を引用する場合 |
| CREW モジュール出力 | 「**CREW ネットワーク分析の媒介中心性**から」 | 発明者・出願人ネットワークの分析結果を引用する場合 |
| NEBULA モジュール出力 | 「**NEBULA の非特許文献分析（論文 / ニュース / 政策）**によると」「**NEBULA 環境分析で導出された仮説**に基づくと」 | 非特許文献の分析結果を引用する場合 |
| ワードクラウド分析 | 「**ワードクラウド分析（Saturn V PROBE クラスタ 4）の特徴語**によると」 | ワードクラウドの特徴語を引用する場合 |
| 空間配置分析（UMAP 座標） | 「**UMAP 空間上の近接関係（x ≈ 3-6, y ≈ 9-13）**から」「**Saturn V TELESCOPE 分析の空間配置**によると」 | 空間配置・距離関係・空間グループの分析を引用する場合 |
| AIインサイト | 「**AI インサイト（Saturn V 解釈）**によると」「**AI インサイトのクラスタ解釈**に基づくと」 | AI インサイトの分析内容を引用する場合（内部ディレクトリ名は書かない） |
| 複数ソースの照合 | 「**Saturn V のクラスタ動態マップと MEGA PULSE の 4 象限を照合した結果**」「**Explorer の共起構造を特許データセットの個別特許で検証した結果**」 | 複数モジュールや特許データセットを横断して分析した場合 |

### 適用ルール

1. **モジュール名は必須**: 全てのデータ引用には APOLLO のモジュール名（Saturn V / Explorer / MEGA / ATLAS / CREW / EAGLE / NEBULA / CORE）を含めること。「マクロ統計データ」「集計データ」等の抽象表現は使用禁止
2. **分析手法名を併記**: 可能な限り、分析手法（SBERT + UMAP + HDBSCAN、Jaccard 共起、CAGR × 活動量 4 象限等）を併記する
3. **特許データセットを直接参照した場合**: フィルタリング・検索した結果を述べる際には、検索条件（「クラスタ 3 に絞込」「A 社の出願のみ」等）を**自然な日本語で**含めたマーカーを付与する。`cluster==3` のようなコード片を本文に出してはいけない（付録のメソドロジー節でのみ許容）
4. **クロスモジュール照合は必須**: 複数モジュールの結果を照合した箇所は、両方のモジュール名を必ず明示する
5. **マーカーの自然な挿入**: 文頭・文中いずれでも可。文章の自然さを損なわない位置に挿入する
6. **⚠️ 絶対禁止**: 内部ファイル名（`*.json`）・内部フィールド名（`spatial_context`, `cluster_dynamics`, `quadrant_summary`, `time_series`, `hype_cycle_phase`, `noise_analysis`, `macro_events`, `npl_gap_analysis` 等）・内部ガイドファイル名（`*.md`）・**内部プロセス用語**（`deep_dive`, `Phase A/B/C/D` 等）をレポート本文に書くこと。`deep_dive` は「モジュール別分析章」「Saturn V 分析章」等に置換する。詳細は `analysis/terminology.md` を参照

### NG例（抽象的なマーカー、内部識別子の露出）

> ❌ マクロ統計データによると、43 クラスタが検出された。集計データでは上位 5 クラスタで全体の 58% を占める。
> ❌ saturnv_clusters.json の spatial_context から、クラスタ 0 と 3 が近接している。
> ❌ mega_momentum.json の quadrant_summary によると、リーダー象限に 3 社が配置されている。

### OK例（具体的なモジュール名・手法名、正式日本語呼称）

> ✅ Saturn V TELESCOPE 分析（SBERT + UMAP + HDBSCAN、min_cluster_size=15）の結果、全 2,623 件から 43 個のクラスタが自動検出された（ノイズ率 12.3%、クラスタ割当率 87.7%）。規模データによると、上位 5 クラスタ（クラスタ 0: 287 件、クラスタ 1: 234 件、クラスタ 2: 198 件、クラスタ 3: 176 件、クラスタ 4: 152 件）で全体の 58.0% を占めており、技術領域の集中度が高いことを示している。

> ✅ Saturn V TELESCOPE 分析の空間配置（UMAP 座標）によると、クラスタ 0「タイヤ用ゴム」とクラスタ 3「薄膜・コーティング」は UMAP 空間上で近接しており、技術的親和性が示唆される。

> ✅ MEGA PULSE 分析の 4 象限サマリによると、旭化成・東ソーはリーダー象限（CAGR > 10%、活動量上位）に配置され、日本製紙・王子製紙は成熟 / 衰退象限に位置している。
