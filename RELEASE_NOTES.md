# リリースノート

## v10.0.0 — 分析者の操縦席（2026-07-25）

v9.1.0 の分析エンジンとレポート品質管理をそのままに、**UI を全面刷新し、分析条件の記録装置を新設**した版です。
姉妹製品 MERCATOR で実証した改良（J-PlatPat 対応・俯瞰図の統一デザイン・多数のバグ修正）を取り込み、
そのうえで **本格的な分析者向けの差別化**（パラメータの前面化・EAGLE の維持・フライトレコーダー・母集団設計を入口に）を加えています。

### 新機能

**フライトレコーダー（分析条件の自動記録）**

- 母集団の来歴（読み込みファイルと件数・結合と重複除去の内訳・除外語数・要約の有無・分類コードが IPC か FI か）と、
  各モジュールの実行条件（粒度・クラスタ数・未分類率・品質指標・乱数シード・所要時間）を自動で記録
- 出力先は3か所 — サイドバーの状態カード（件数）／画面の記録カード／ZIP 内 `flight_recorder.json`／レポート付録の「分析条件」表
- **記録は全パラメータ、レポートへの出し方は3層**。層は JSON 内の `layer_policy` に明示して運ぶ
  - 層1 `lineage`（母集団の来歴）→ レポート**本文で使える**
  - 層2 `judgment`（粒度・クラスタ数・未分類率・DBCV 等）→ **「結論の前提と見直しのサイン」の材料**。数値ではなく条件文に変換して書く
  - 層3 `repro`（近傍数・最小距離・乱数シード・モデル名・所要秒数）→ **付録の表にだけ**。本文への数値記載は禁止
- `capcom_schema/SKILL.md` の Phase A チェックリストと `analysis/report_structure.md` の付録A テンプレートを、
  推定値ではなく記録された実値から書く手順に更新

**母集団設計を入口に**

- 検索式・その設計意図・収録年・使用したデータベース名の記録を、Mission Control の先頭タブ「0. 母集団設計」へ移設
  （従来はレポート生成ページの奥）。入力は即座にフライトレコーダーの層1へ反映される
- CAPCOM ページには記録状況の確認と編集への導線を残した。session_state のキー名は ZIP スキーマ互換のため不変

**パラメータの前面化（Saturn V）**

- 最小クラスタサイズ・最小サンプル数・目標クラスタ数・**乱数シード**を実行バーへ昇格。詳細設定は既定で開く
- 地図の品質（クラスタ数・未分類率・DBCV・採用した粒度）を結果の隣に常時表示
- 乱数シードを変えて再描画すると座標を計算し直す。配置が変わっても同じ塊が見えるか＝**結論が配置に依存していないか**を確かめられる

### UI 刷新

- **正式名称（バクロニム）を明示** — Advanced Patent & Overall Landscape-analytics Logic Orbiter。サイドバーのロックアップに表示
- **表記を実務者向けに統一** — 進捗表示を処理内容の記述に（「特許の内容をAIが読み込んでいます」→
  「文埋め込みの生成（SBERT）」）、ページ副題を分析手法の記述に、平易化されていた語を実務語へ
  （「分析の準備」→「前処理」等）。**画面から装飾絵文字を全廃**し、アイコンは Material Symbols に統一
- **EAGLE を Saturn V の直下に配置** — 自動クラスタリングの結果を手で引き直すモジュールのため。
  「上級」の表記は付けない
- **グラファイトの計器盤** — サイドバーを暗色に、操作するコンテンツ領域は白基調のまま。
  図の描画領域を白に保つのは、俯瞰図の12色パレットが白地で色覚検証されているため
- **軌道ロゴマーク** — サイドバーのロックアップとページ見出しに配置。ファビコンも軌道マークに刷新
- **線アイコンに統一** — ナビの絵文字を全廃し Material Symbols へ。表示はコードネームが主、機能名を小さく副える
- **3グループナビ** — 「① 母集団 → ② 分析 → ③ レポート」（`st.navigation`）。
  データ状態カード（ファイル名・件数・前処理状態・📸枚数・飛行記録件数）を常駐表示
- **「この図の見方」カード** — 主要な図の直下に設置。既定は折りたたみ（熟練者の視界を塞がない）
- **統一データガード** — どのページでも同じ案内と「Mission Control を開く」ボタン
- 共通UIは `apollo_ui.py` に集約。ビューは `views/*.py` の `render()` を公開する形に再構成

### 取込・分析（MERCATOR からの取り込み）

- **J-PlatPat CSV 対応** — 検索結果CSVの自動認識（要約なし形式にも対応）、分割ダウンロードした複数ファイルの
  まとめて読込（自動結合・重複除去）、必須カラムの緩和（発明の名称・出願番号・出願日・出願人の4つ）、
  分類コードの IPC / FI 両対応と実効表記の動的化、外字記号（▲▼）除去、要約の定型ヘッダ除去、文献URL からの原文リンク
- **俯瞰図の統一デザイン** — 俯瞰図ファミリー4箇所（Saturn V メイン／ドリルダウン・NEBULA 学術・MEGA ドリル）を
  同一デザインに刷新。離散固定色（色覚シミュレーションと OKLab 色差で並び順を最適化した12色）・
  点と勢力圏とラベルの色一致・平滑な勢力圏・クラスタ別の地形図風密度・件数上位クラスタのラベル拡大
- **権利化率に審判系を算入** — 登録後の争い（異議申立・無効審判）は権利化成功に算入、
  登録前（拒絶査定不服）は未確定として区別

### レポート生成

- **本文の呼称を機能名に統一** — 本文・別冊・スライドの分析名を機能名（俯瞰図分析・動態分析 等）で出力する。
  読者はモジュールのコードネームを知らないため。品質ゲートに **Check 8g**（コードネーム露出の警告）を追加。
  ブランド名 APOLLO は表紙・付録で名乗るため検出対象から除外
- **チャットキット** — プロンプト＋証拠画像を1クリックでパッケージし、任意のチャットAIに貼るだけで
  レポート骨格を生成（APIキー不要）。応答を貼り戻して読む専用ビューア付き
- **CAPCOM 1ページに統合** — 旧 VOYAGER + CAPCOM を統合し、Export→ZIP の2段操作を1ボタン化
- **記録セッションの自動開始** — 前処理完了時に自動で開始

### 廃止

- **Gemini API による自動レポート（旧 VOYAGER）** — LLM API 依存を持たない保守フリー原則により削除。
  同じ役割はチャットキットが引き継いだ。`requirements.txt` から `google-generativeai` を除去。
  ZIP 内部の `voyager/` パス名・キー名は互換のため不変

### 修正

- 基本統計のバークリックで**別タブの図に化ける**（`@st.fragment` がローカル変数 `fig` をクロージャ参照しており、
  部分再実行時に後続タブの図を描いていた。既定引数で束縛して解決）
- 勢力圏と点の**座標ズレ**（`Scattergl`＝WebGL と凸包＝SVG の混在。`Scatter` に統一）— Saturn V / MEGA / EAGLE
- **ステータス空欄の特許が内訳グラフから消える**（groupby の NaN 落ちで総数と食い違っていた。
  `STATUS_UNKNOWN_LABEL`「(ステータス不明)」を新設し内訳集計を fillna で統一）
- **密度マップの端が四角く切れる**（メッシュ範囲がデータの min/max ちょうどだった。両側に余白ビンを確保）
- 「密度スケール固定」チェックが未配線のデッドUIだった → 全期間基準の共通 zmax として配線
- MEGA ドリルダウンの点色が虹色（`turbo`）で、クラスタの色分けと一致していなかった
- CREW で**出願人を発明者用の区切り文字で分割**しており、共同出願が0件になっていた（出願人用の区切り文字を分離指定）
- CREW の発明者カラム選択に「(なし)」が無く、未設定時に先頭カラムへフォールバックしていた
- 関数内の `import` 再宣言（`UnboundLocalError` の罠）をモジュール先頭へ一本化

### 互換性

- 分析エンジン（patiroha）・レポート品質ゲート（Check 1〜37）・対話型モード（KATHERINE）・
  ZIP セッション構造は v9.1.0 と互換。ZIP 内部のファイル名・データ構造・`module` 値・session_state キー名は不変
- `flight_recorder.json` と `voyager/context.json` への追加フィールド
  （`classification_code_label` / `abstract_available`）は後方互換な追加
- レポート本文の分析名は機能名で出力される（旧版で生成したレポートには影響しない）

---

## v9.1.0 以前

v9.1.0（対話型レポート作成モード KATHERINE）・v9.0.0（分析の信頼性）・v8.0.0 のリリースノートは
[GitHub Releases](https://github.com/shibayamalicht/apollo-patent-analysis/releases) を参照してください。
