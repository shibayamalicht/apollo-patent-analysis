# ======================================================================
# APOLLO v9 CAPCOM セッション専用 — レポート/スライド生成用の最小依存
# ======================================================================
#
# このファイルは CAPCOM ZIP に同梱される。ZIP 展開後のセッションフォルダで:
#
#   pip install -r requirements-session.txt
#
# を実行すれば、Claude Code / Codex / Antigravity で以下の生成が可能になる:
#   - patents.csv の解析  (出願人上位・クラスタ別件数・代表特許検索 → pandas)
#   - PowerPoint 生成     (apollo-pptx スキル → python-pptx)
#   - 画像の埋め込み       (snapshots/*.png を本文に挿入 → Pillow)
#
# Streamlit アプリ本体（Home.py）を動かすための依存は含まない。
# それは apollo_v8/requirements.txt 側にある。
# ======================================================================

pandas
python-pptx
Pillow
